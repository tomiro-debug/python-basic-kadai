# 課題：関数を使いこなそう

商品を購入して、消費税を加えた計算結果を返す関数を記述してください。
第1引数に商品の金額、第2引数に消費税（10%）を設定できるようにしてください。

def calculate_total(price, tax): # 商品を買う関数の設定。
    total = price + price * tax  # 計算結果を変数totalに代入する。
    return total # 計算結果を返す

calculate_total(1000, 0.1) # 関数を呼び出し、引数を商品の金額と消費税に渡す。
